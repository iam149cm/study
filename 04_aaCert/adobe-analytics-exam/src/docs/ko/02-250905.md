### ✅ 간단 개념 학습: Context Data & Processing Rules

#### ☝🏻 Context Data란?
- Web SDK 또는 AppMeasurement로 이벤트 전송 시, `contextData` 객체를 통해 커스텀 데이터를 함께 보낼 수 있음.
- 예: 
  ```js
  s.contextData['product.category'] = "Smartphone";
  s.contextData['product.color'] = "Black";
  ```

#### ✌🏻 Processing Rules란?
- Adobe Analytics Admin 콘솔에서 설정.
- Context Data → eVar, prop, event 등 보고서 변수로 매핑.
- 코드 수정 없이도 데이터 수집 로직을 조정할 수 있어 유지보수성이 뛰어남.

---

### 📊 데이터 흐름 다이어그램
```
Context Data (s.contextData['key'] = value)
        ↓
Processing Rule (map 'key' → eVar / prop / event)
        ↓
Report Suite → Analytics Report
```

---

### 📝 네이밍 Best Practice
- key는 소문자, 공백·특수문자 피하기  
- 네임스페이스 구조 사용 권장 (`category.productType`)  
- 일관성 있는 네이밍으로 여러 Report Suite에서 공통 활용 가능  

---

### ✅ 실무 포인트
- **데이터는 Debugger에 보이지만 보고서에 안 잡히는 경우 → 대부분 Processing Rule 미매핑**  
- 체크리스트:
  1. Context Data key 이름이 올바른가?  
  2. Processing Rule 매핑이 존재하는가?  
  3. 올바른 Report Suite에 데이터가 전송되었는가?  
  4. Rules 순서 충돌은 없는가?  

---

### 🎯 예상 문제

**Q1.** Context Data was successfully sent but does not appear in reports. What is the most likely reason?  

    A. ECID misconfiguration  
    B. Processing Rule mapping error  
    C. Missing consent update  
    D. Wrong timezone setting  

- **정답:** B  
- **해설:** Context Data는 자동으로 보고서에 잡히지 않으며, 반드시 Processing Rule을 통해 매핑해야 한다.  

---

**Q2.** A contextData variable `productCategory` is successfully sent, but eVar10 in the report is empty. What is the most likely cause?  

    A. Report Suite mismatch  
    B. Processing Rule not configured  
    C. ECID missing  
    D. Data Insertion API disabled  
    
- **정답:** B  
- **해설:** Context Data는 eVar와 직접 연결되지 않는다. Analytics 보고서 변수에 값이 보이려면 반드시 Processing Rule에서 매핑해야 한다.  

---

**Q3.** Multiple contextData keys are sent, but only some values are showing in reports. What should be checked first?  

    A. Processing Rule priority and conflicts  
    B. Browser cache  
    C. ECID expiration  
    D. Report timezone  

- **정답:** A  
- **해설:** Processing Rules는 위에서 아래 순서대로 적용되므로, 충돌하거나 덮어쓰는 Rule이 있으면 일부 값이 반영되지 않는다.  
