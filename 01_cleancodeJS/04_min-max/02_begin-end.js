/**
 * 에어비엔비의 케이스 (달력)
 * 시작 날짜 (체크인) = begin
 * 끝 날짜 (체크아웃) = end
 *
 * 1. 체크인 날짜는 체크아웃 날짜보다 빨라야 한다.
 * 2. 체크아웃 날짜는 체크인 날짜보다 늦어야 한다.
 * 3. 체크인 날짜와 체크아웃 날짜는 같을 수 없다.
 */

function reservationDate(beginDate, endDate) {
    // ...some code
}

reservationDate('2020-10-11', '2020-10-12'); // 1박