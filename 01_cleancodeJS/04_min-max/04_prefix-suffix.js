/** 
 * prefix = 접두사. 앞에 붙는 것. 
 *  ex) getter/setter ... useState/useEffect ... jquery 의 $, js 의 #, _ 등
 * 
 * suffix = 접미사. 뒤에 붙는 것
 *  ex) REDUX 의 action type 의 경우, _REQUEST, _SUCCESS, _FAILURE 등
 *      복수단위로 관리되는 파일은 s가 붙는다. ex) actions, reducers, containers, components, pages, ...
 */